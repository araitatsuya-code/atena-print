# 次の Issue を取得して実装開始

次に取り組むべき Issue を見つけて実装を開始してください。

手順:

1. `gh issue list --state open --label <現在のPhase> --limit 1` で次の Issue を取得
2. Issue がなければ次の Phase のラベルで探す
3. 見つかったら /project:work-issue と同じフローで実装開始

もし open な Issue がなければ「全 Issue 完了」と報告してください。

---

# Issue を実装

指定された GitHub Issue を実装してください。

## 手順

1. `gh issue view <番号>` で Issue の内容を確認
2. ブランチを作成: `git checkout -b issue-<番号>-<slug>`
3. 必要な仕様を docs/ から参照して実装
4. テスト実行:
   - Go: `go test ./internal/...`
   - Frontend: `cd frontend && npx vitest --run`
5. 変更をコミット (コミットメッセージに `refs #<番号>` を含める)
6. Push して PR 作成:

ARGUMENTS: 6

---

Verify each finding against the current code and only fix it if needed.

Inline comments:
In `@frontend/src/App.tsx`:
- Around line 9-12: App is re-rendering on every store change because
useContactStore() subscribes to whole store; change to selector-based
subscription to only pick contacts and selectedIds (e.g., call
useContactStore(state => ({ contacts: state.contacts, selectedIds:
state.selectedIds }), useShallow) or equivalent) so App only updates when those
two values change; update the variable destructuring in App (contacts,
selectedIds) and keep previewContact calculation (previewContact =
contacts.find(...)) unchanged.

---

コミットしてpushして

---

マージしたのでissueやdocsの状態を変更してください